This directory is specifically ignored by git so a server instance can store private, instance-specific data here. This includes:
* SSL certificate information
* Logs
* Database dumps
* Password files
