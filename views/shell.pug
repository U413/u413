.shell
	|#[.user= user.name]@#[.host u413.com]:
	.cwd= user.cwd
	.prompt= user.prompt || "$"
	| 
	input#cli(
		autofocus
		autocomplete="off" autocorrect="off"
		autocapitalize="off" spellcheck="false"
	)
	script(src="/etc/scripts/shell.js")
